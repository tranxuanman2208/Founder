<Window x:Class="trangchu.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:trangchu"
        xmlns:views="clr-namespace:trangchu.Views"
        Title="Demo Navigation" Height="500" Width="900">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="141*"/>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="26*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <StackPanel.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black"/>
                    <GradientStop Color="White" Offset="1"/>
                </LinearGradientBrush>
            </StackPanel.Background>
            <Rectangle Height="100">
                <Rectangle.Fill>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Black"/>
                        <GradientStop Color="White" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <Button x:Name="btnHome" Content="TRANG CHỦ" Height="40"
                    Click="MenuClick" 
                    Background="{x:Null}" BorderBrush="{x:Null}"
                    Foreground="White" FontSize="16" FontWeight="Bold"/>

            <Button x:Name="btnBasic" Content="CƠ BẢN" Height="40"
                    Click="MenuClick" 
                    Background="{x:Null}" BorderBrush="{x:Null}"
                    Foreground="White" FontSize="16" FontWeight="Bold"/>

            <Button x:Name="btnFunc1" Content="TÍNH NĂNG 1" Height="40"
                    Click="MenuClick" 
                    Background="{x:Null}" BorderBrush="{x:Null}"
                    Foreground="White" FontSize="16" FontWeight="Bold"/>

            <Button x:Name="btnFunc2" Content="TÍNH NĂNG 2" Height="40"
                    Click="MenuClick" 
                    Background="{x:Null}" BorderBrush="{x:Null}"
                    Foreground="White" FontSize="16" FontWeight="Bold"/>
        </StackPanel>

        <Border Grid.Column="1" Margin="10,10,10,10" Background="#EEE" Grid.ColumnSpan="3">
            <ContentControl x:Name="ContentArea">
                <ContentControl.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Black"/>
                        <GradientStop Color="White" Offset="1"/>
                    </LinearGradientBrush>
                </ContentControl.Background>
            </ContentControl>
        </Border>
    </Grid>
</Window>
